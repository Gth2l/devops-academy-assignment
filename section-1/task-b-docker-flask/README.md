# Dockerized Flask App

This project contains a simple Flask web application packaged into a Docker container.

## Run

docker compose up --build

## Test

- http://localhost:5000
- http://localhost:5000/healthz
