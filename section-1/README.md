# Section 1 – Implementation

This section is reserved for hands-on implementation tasks related to infrastructure, containers, and Kubernetes.

## Available Tasks

- `task-a-terraform-vpc/` – Terraform-managed AWS VPC
- `task-b-docker-flask/` – Dockerized Python Flask web application
- `task-c-kubernetes-helm/` – Kubernetes application deployment with Helm

## Notes

Only one task from this section is required for the assignment.  
The folder structure is prepared for completeness and repository organization.
